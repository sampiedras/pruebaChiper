import { StyleSheet } from 'react-native';

export default () => {
  return StyleSheet.create({
    container: {
      flex: 1,
      width: '100%',
      height: '100%',
    },
    text: {
      marginTop: 20,
      textAlign: 'center',
    },
  });
};
